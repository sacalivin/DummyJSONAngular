import { EmailConfirmationValidationDirective } from './email-confirmation-validation.directive';

describe('EmailConfirmationValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailConfirmationValidationDirective();
    expect(directive).toBeTruthy();
  });
});
