import { IdentificationDocumentValidatorDirective } from './identification-document-validator.directive';

describe('IdentificationDocumentValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new IdentificationDocumentValidatorDirective();
    expect(directive).toBeTruthy();
  });
});


