import { CheckPaymentStatus } from './check-payment-status';

describe('CheckPaymentStatus', () => {
  it('should create an instance', () => {
    expect(new CheckPaymentStatus()).toBeTruthy();
  });
});
